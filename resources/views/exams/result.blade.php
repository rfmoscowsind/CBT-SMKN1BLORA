@extends('layouts.app') @section('content')<div class=card style=max-width:640px;margin:auto;text-align:center><h1>Ujian Selesai</h1><p>{{ $session->schedule->title }}</p>@if($session->schedule->show_score)<div class=stat>{{ number_format($session->score,2) }}</div><p>Nilai akhir</p>@else<p>Nilai akan diumumkan oleh guru.</p>@endif<p class=muted>Terjawab: {{ $session->answers->whereNotNull('answer')->count() }} / {{ $session->schedule->questions->count() }}</p><a class=btn href={{ route('dashboard') }}>Kembali ke Dashboard</a></div>@endsection
